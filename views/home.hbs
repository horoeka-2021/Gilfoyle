<div class="container">
  {{#each movies}}
    <div class="puppy-list">
      {{!-- <a href="/puppies/{{id}}"> --}}
      <img
          class="img-circle"
          src="{{image}}"
          alt="{{title}}"
        /></a>
    </div>
  {{/each}}
</div>